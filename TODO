- githooks run post-merge/hook-name
  - Script location (usually ~/.githooks, settable in tests)
  - Environment variables for all different cases (separate object for each hook type?)

- githooks fetch (to cache)
  - With local file
  - With remote location

- githooks install + githooks list
  - put a githooks run into the git hook file
  - list all hooks
