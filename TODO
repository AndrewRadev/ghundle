- githooks fetch (to cache)
  - With remote location

- githooks list
  - list project hooks
  - list all hooks

- Githooks::Metadata to wrap hook metadata, its location, provide validations
- Githooks::Hook to wrap hook files, paths, installed or not, metadata

- Lots of documentation

Later:
  - Double-check all git hook types for env variables and stuff (also write tests)
