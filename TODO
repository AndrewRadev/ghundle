- githooks fetch (to cache)
  - With local file
  - With remote location

- githooks install + githooks list
  - put a githooks run into the git hook file
  - list all hooks


Later:
  - Double-check all git hook types for env variables and stuff (also write tests)
